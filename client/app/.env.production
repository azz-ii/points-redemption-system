# Production configuration - Using IIS reverse proxy to Django backend
# Empty VITE_API_URL makes the app use relative paths (/api/*)
# IIS will forward these to localhost:8000 via reverse proxy
VITE_API_URL=
VITE_API_BASE_URL=

# Note: Django backend runs on localhost:8000 (NSSM service)
# IIS handles reverse proxy from /api/* to http://localhost:8000/api/*

# Localhost configuration (for local development/testing)
# VITE_API_URL=http://localhost:8000
# VITE_API_BASE_URL=http://localhost:8000

# Alternative for Create React App (if used)
REACT_APP_API_URL=http://points-redemption.n01tb.com:8000
REACT_APP_API_BASE_URL=http://points-redemption.n01tb.com:8000
